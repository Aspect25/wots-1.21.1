# Luminite Block — Shader Item Properties
# Format follows Just Colored Lighting spec: modid:itemname
# Color: texture_medium — matches the block light color when held

# Just Colored Lighting (texture color match, medium brightness)
item.10002 = wots:luminite_block

# Complementary Reimagined / Unbound (purple preset)
item.10216 = wots:luminite_block

# BSL
item.10100 = wots:luminite_block
